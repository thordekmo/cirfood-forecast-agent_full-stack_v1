# Paths
DATA_DIR=/app/data
ARTIFACTS_DIR=/app/artifacts

# App
APP_VERSION=0.2.0
PORT=8000

# Defaults for /jobs/run
FREQUENCY=W
HORIZON=8

# CORS
ALLOWED_ORIGINS=http://localhost:5173

# Enable POST /data/upload from the UI
ENABLE_DIRECT_UPLOAD=true
